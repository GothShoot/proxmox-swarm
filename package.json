{
  "name": "proxmox-swarm",
  "version": "0.1.0",
  "description": "CLI wrapper for Proxmox in TypeScript",
  "main": "dist/cli.js",
  "scripts": {
    "build": "tsc",
    "start": "node dist/cli.js"
  },
  "dependencies": {
    "commander": "^11.0.0"
  },
  "devDependencies": {
    "@types/node": "^24.3.0",
    "typescript": "^5.4.0"
  }
}
